// NOTE: DELETE ME!
// In other words, put in your keys and configuration, then delete

const config = {
    dev_to_api_key : 'dev.to API key',
    transport_service : 'Fake',
    transport_host : 'smtp.fake.com',
    transport_email : 'node mailer email',
    transport_password : 'email password',
}

export default config;